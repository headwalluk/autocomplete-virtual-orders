<?xml version="1.0"?>
<ruleset name="Autocomplete Virtual Orders Standards">
	<description>WordPress Coding Standards for Autocomplete Virtual Orders</description>

	<file>.</file>

	<!-- Exclude patterns -->
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/assets/*</exclude-pattern>
	<exclude-pattern>*/.git/*</exclude-pattern>
	<exclude-pattern>*/build/*</exclude-pattern>
	<exclude-pattern>*/dev-notes/*</exclude-pattern>
	<exclude-pattern>*/docs/*</exclude-pattern>

	<!-- Arguments -->
	<arg value="sp"/>
	<arg name="basepath" value="."/>
	<arg name="colors"/>
	<arg name="extensions" value="php"/>
	<arg name="parallel" value="8"/>

	<!-- Rules -->
	<rule ref="WordPress">
		<exclude name="Generic.Arrays.DisallowShortArraySyntax"/>

		<!-- An if with elseif branches ends in an explicit else, with no-op branches
		     holding only a comment (see CLAUDE.md, PHP Style). This sniff counts a comment as
		     empty, so only its if/elseif/else codes are excluded; empty catch, loop and
		     switch bodies are still errors. -->
		<exclude name="Generic.CodeAnalysis.EmptyStatement.DetectedIf"/>
		<exclude name="Generic.CodeAnalysis.EmptyStatement.DetectedElseif"/>
		<exclude name="Generic.CodeAnalysis.EmptyStatement.DetectedElse"/>
	</rule>

	<!-- Text domain -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="autocomplete-virtual-orders"/>
			</property>
		</properties>
	</rule>

	<!-- Prefixes -->
	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="autocomplete_virtual_orders"/>
				<element value="acvo"/>
				<element value="Autocomplete_Virtual_Orders"/>
				<element value="ACVO"/>
			</property>
		</properties>
	</rule>
</ruleset>
